# ==============================================================================
#  SERVER MODULE — XL-MS (MeroX + xiSearch + network visualization)
#  Called from app.R as: server_xlms(input, output, session, values, add_to_log)
#  Status: Stub — no pipeline logic yet
# ==============================================================================

server_xlms <- function(input, output, session, values, add_to_log) {

  # Placeholder — XL-MS server logic will be implemented here
  # Reactive namespace: xlms$ (separate from DIA values$fit / values$y_protein)

}
